<?xml version="1.0"?>

<launch>

    <rosparam file="$(find marm_gazebo)/config/joint_trajectory_controllers.yaml" command="load"/>

     <node name="arm_controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
          output="screen" ns="/arm" args="arm_trajectory_controller gripper_trajectory_controller"/>

</launch>